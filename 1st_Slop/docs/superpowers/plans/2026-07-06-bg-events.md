# Événements de fond par décor — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Un événement signature procédural par décor (rafale néon, torchère, vol d'oiseaux, frappe de foudre, étoile filante), rare mais spectaculaire, dessiné par-dessus les PNG statiques.

**Architecture:** Module logique pur `src/game/bgevents.js` (planificateur + courbes, même famille que `twinkle.js`/`ambiance.js`), module de dessin `src/render/bgevents.js` appelé entre le fond lointain et le premier plan, intégration `world.js` (création, update, reset au changement de décor). Spec : `docs/superpowers/specs/2026-07-06-bg-events-design.md`.

**Tech Stack:** Vanilla JS + Canvas 2D, Vitest, smoke Playwright (import via `file:///C:/Users/pattyn/AppData/Local/npm-cache/_npx/e41f203b7505f1fb/node_modules/playwright/index.mjs`).

## Global Constraints

- **Timing** : n'implémenter qu'APRÈS la cascade de merge tier-decors → theme-music → player-progression vers main. Branche `feat/bg-events` depuis main mergé, dans un worktree isolé (skill superpowers:using-git-worktrees).
- Cadence : un événement toutes les **6 à 12 s** (uniforme via `rand`), **un seul actif à la fois**.
- Flash foudre plafonné : alpha **≤ 0,35**, jamais de blanc plein écran.
- Zéro nouvel asset, zéro dépendance runtime.
- Couche de dessin : entre le fond lointain (étape 1 du renderer) et le premier plan (étape 2) ; exception rafale néon = boost des twinkles existants (étape 2a).
- Changement de décor (`applyBgSet`) : événement en cours coupé, timer réarmé.
- Update partout où le décor est visible (même politique qu'`updateAmbiance` : aussi au MENU).
- Style : code + commentaires en français, modules purs sans DOM, commits conventionnels français.

---

### Task 1: Module logique `bgevents.js` (planificateur + courbes)

**Files:**
- Create: `src/game/bgevents.js`
- Test: `tests/game/bgevents.test.js`

**Interfaces:**
- Consumes: `CONFIG.WIDTH` (`src/config.js`).
- Produces: `createBgEvents(rand) -> {timer, event: null}` ; `updateBgEvents(ev, dt, bgSet, rand)` (mutation) ; `resetBgEvents(ev, rand)` ; `foudreAlpha(event) -> number` ; `rafaleAlpha(ev, x) -> number`. L'événement actif est `{kind, t, dur, ...params}` avec `kind` ∈ `rafale|torchere|oiseaux|foudre|etoile`.

- [ ] **Step 1: Écrire les tests qui échouent**

Créer `tests/game/bgevents.test.js` :

```js
import { describe, it, expect } from 'vitest';
import { createBgEvents, updateBgEvents, resetBgEvents, foudreAlpha, rafaleAlpha } from '../../src/game/bgevents.js';

const zero = () => 0;   // délai minimal 6 s, params déterministes
const half = () => 0.5; // délai 9 s

describe('bgevents planificateur', () => {
  it('crée un timer dans [6, 12) et aucun événement', () => {
    expect(createBgEvents(zero).timer).toBe(6);
    expect(createBgEvents(() => 0.999).timer).toBeCloseTo(11.994, 3);
    expect(createBgEvents(zero).event).toBe(null);
  });

  it('déclenche l événement du décor courant à échéance', () => {
    const ev = createBgEvents(zero);
    updateBgEvents(ev, 6, 3, zero);
    expect(ev.event).toMatchObject({ kind: 'foudre', t: 0, dur: 0.5 });
  });

  it('chaque bgSet déclenche son kind', () => {
    const kinds = ['rafale', 'torchere', 'oiseaux', 'foudre', 'etoile'];
    for (let bg = 0; bg < 5; bg += 1) {
      const ev = createBgEvents(zero);
      updateBgEvents(ev, 6, bg, zero);
      expect(ev.event.kind).toBe(kinds[bg]);
    }
  });

  it('un seul événement actif : pas de re-déclenchement pendant', () => {
    const ev = createBgEvents(zero);
    updateBgEvents(ev, 6, 3, zero);
    const started = ev.event;
    updateBgEvents(ev, 0.1, 3, zero);
    expect(ev.event).toBe(started);
    expect(ev.event.t).toBeCloseTo(0.1, 5);
  });

  it('meurt à dur écoulée et réarme le timer', () => {
    const ev = createBgEvents(zero);
    updateBgEvents(ev, 6, 3, zero);
    updateBgEvents(ev, 0.5, 3, half);
    expect(ev.event).toBe(null);
    expect(ev.timer).toBe(9);
  });

  it('resetBgEvents coupe l événement en cours et retire un délai', () => {
    const ev = createBgEvents(zero);
    updateBgEvents(ev, 6, 4, zero);
    expect(ev.event).not.toBe(null);
    resetBgEvents(ev, half);
    expect(ev.event).toBe(null);
    expect(ev.timer).toBe(9);
  });
});

describe('courbes', () => {
  it('foudreAlpha : pic 0.35, second pulse plus faible, retombe à ~0', () => {
    expect(foudreAlpha({ t: 0 })).toBeCloseTo(0.35, 5);
    const second = foudreAlpha({ t: 0.26 });
    expect(second).toBeGreaterThan(0);
    expect(second).toBeLessThan(0.35);
    expect(foudreAlpha({ t: 0.5 })).toBeLessThanOrEqual(0.01);
  });

  it('foudreAlpha ne dépasse jamais 0.35 (portes lisibles)', () => {
    for (let t = 0; t <= 0.5; t += 0.01) {
      expect(foudreAlpha({ t })).toBeLessThanOrEqual(0.35);
    }
  });

  it('rafaleAlpha : 0 sans événement, 1 sur le front, 0 loin du front', () => {
    const ev = createBgEvents(zero);
    expect(rafaleAlpha(ev, 100)).toBe(0);
    updateBgEvents(ev, 6, 0, zero);   // rafale démarre (dur 1.2)
    updateBgEvents(ev, 0.6, 0, zero); // t=0.6 -> front à (0.5)*(360+80)-40 = 180
    expect(rafaleAlpha(ev, 180)).toBeCloseTo(1, 2);
    expect(rafaleAlpha(ev, 300)).toBe(0);
  });
});
```

- [ ] **Step 2: Vérifier qu'ils échouent**

Run: `npx vitest run tests/game/bgevents.test.js`
Expected: FAIL — `Cannot find module '../../src/game/bgevents.js'`

- [ ] **Step 3: Implémenter le module**

Créer `src/game/bgevents.js` :

```js
import { CONFIG } from '../config.js';

// Un événement signature par décor (spec 2026-07-06-bg-events-design).
// Cadence : un déclenchement toutes les 6-12 s, un seul actif à la fois.
const DELAY_MIN = 6;
const DELAY_RANGE = 6;

// Sommets de cheminées sur bg-far-1 (canvas 360×640) — à ajuster au
// vérif visuel si le halo ne tombe pas pile sur les fumées vertes.
const TORCH_SPOTS = [
  { x: 46, y: 268 }, { x: 78, y: 248 }, { x: 330, y: 300 },
];

// Générateur de params par décor. Durées courtes = événement ponctuel ;
// les oiseaux traversent lentement (leur durée EST la traversée).
const EVENTS = [
  (rand) => ({ kind: 'rafale', dur: 1.2 }),
  (rand) => ({ kind: 'torchere', dur: 2.5, spot: TORCH_SPOTS[Math.floor(rand() * TORCH_SPOTS.length)] }),
  (rand) => ({ kind: 'oiseaux', dur: 9, baseY: 150 + rand() * 110, dir: rand() < 0.5 ? 1 : -1 }),
  (rand) => ({ kind: 'foudre', dur: 0.5 }),
  (rand) => ({
    kind: 'etoile', dur: 0.7,
    x0: 40 + rand() * 200, y0: 30 + rand() * 150,
    vx: 260 + rand() * 80, vy: 110 + rand() * 50,
  }),
];

function drawDelay(rand) {
  return DELAY_MIN + rand() * DELAY_RANGE;
}

export function createBgEvents(rand) {
  return { timer: drawDelay(rand), event: null };
}

export function resetBgEvents(ev, rand) {
  ev.event = null;
  ev.timer = drawDelay(rand);
}

export function updateBgEvents(ev, dt, bgSet, rand) {
  if (ev.event) {
    ev.event.t += dt;
    if (ev.event.t >= ev.event.dur) {
      ev.event = null;
      ev.timer = drawDelay(rand);
    }
    return;
  }
  ev.timer -= dt;
  if (ev.timer <= 0) {
    ev.event = { t: 0, ...EVENTS[bgSet](rand) };
  }
}

// Flash foudre : double pulse (attaque instantanée, retombée rapide,
// second coup plus faible), plafonné à 0.35 — les portes restent lisibles.
export function foudreAlpha(event) {
  const p1 = Math.max(0, 1 - event.t / 0.18);
  const p2 = event.t < 0.24 ? 0 : Math.max(0, 1 - (event.t - 0.24) / 0.2);
  return 0.35 * p1 + 0.22 * p2;
}

// Rafale néon : un front balaie l'écran de gauche à droite, les fenêtres
// twinkle proches du front passent à pleine intensité (renderer étape 2a).
export function rafaleAlpha(ev, x) {
  const e = ev.event;
  if (!e || e.kind !== 'rafale') return 0;
  const front = (e.t / e.dur) * (CONFIG.WIDTH + 80) - 40;
  const d = Math.abs(x - front);
  return d < 40 ? 1 - d / 40 : 0;
}
```

- [ ] **Step 4: Vérifier que les tests passent**

Run: `npx vitest run tests/game/bgevents.test.js`
Expected: PASS (9 tests)

- [ ] **Step 5: Commit**

```bash
git add src/game/bgevents.js tests/game/bgevents.test.js
git commit -m "feat(bgevents): planificateur d'événements de fond par décor + courbes foudre/rafale"
```

---

### Task 2: Module de dessin `render/bgevents.js`

**Files:**
- Create: `src/render/bgevents.js`
- Test: `tests/render/bgevents.test.js`

**Interfaces:**
- Consumes: `world.bgEvents` (Task 1), `foudreAlpha` (Task 1), `CONFIG.WIDTH/HEIGHT`.
- Produces: `renderBgEvents(ctx, world)` — dessine l'événement actif ; no-op si aucun ou si `kind === 'rafale'` (géré par le boost twinkles en Task 3).

- [ ] **Step 1: Écrire les tests qui échouent**

Créer `tests/render/bgevents.test.js` :

```js
import { describe, it, expect } from 'vitest';
import { renderBgEvents } from '../../src/render/bgevents.js';
import { CONFIG } from '../../src/config.js';

function fakeCtx() {
  const calls = [];
  return {
    calls,
    globalAlpha: 1,
    fillStyle: '',
    fillRect: (...a) => calls.push(a),
  };
}

function worldWith(event) {
  return { bgEvents: { timer: 6, event } };
}

describe('renderBgEvents', () => {
  it('ne dessine rien sans événement actif', () => {
    const ctx = fakeCtx();
    renderBgEvents(ctx, worldWith(null));
    expect(ctx.calls.length).toBe(0);
  });

  it('foudre : un voile plein cadre, alpha extrait de la courbe (≤ 0.35)', () => {
    const ctx = fakeCtx();
    renderBgEvents(ctx, worldWith({ kind: 'foudre', t: 0, dur: 0.5 }));
    expect(ctx.calls).toEqual([[0, 0, CONFIG.WIDTH, CONFIG.HEIGHT]]);
    const alpha = Number(ctx.fillStyle.match(/rgba\(210,225,255,(.+)\)/)[1]);
    expect(alpha).toBeCloseTo(0.35, 3);
  });

  it('étoile : tête + traînée (7 points), globalAlpha restauré à 1', () => {
    const ctx = fakeCtx();
    renderBgEvents(ctx, worldWith({ kind: 'etoile', t: 0.2, dur: 0.7, x0: 60, y0: 40, vx: 260, vy: 110 }));
    expect(ctx.calls.length).toBe(7);
    expect(ctx.globalAlpha).toBe(1);
  });

  it('oiseaux : 5 oiseaux × 3 rects', () => {
    const ctx = fakeCtx();
    renderBgEvents(ctx, worldWith({ kind: 'oiseaux', t: 1, dur: 9, baseY: 200, dir: 1 }));
    expect(ctx.calls.length).toBe(15);
  });

  it('torchère : 3 halos concentriques sur le spot', () => {
    const ctx = fakeCtx();
    renderBgEvents(ctx, worldWith({ kind: 'torchere', t: 1, dur: 2.5, spot: { x: 46, y: 268 } }));
    expect(ctx.calls.length).toBe(3);
    expect(ctx.globalAlpha).toBe(1);
  });

  it('rafale : no-op ici (gérée par le boost des twinkles)', () => {
    const ctx = fakeCtx();
    renderBgEvents(ctx, worldWith({ kind: 'rafale', t: 0.5, dur: 1.2 }));
    expect(ctx.calls.length).toBe(0);
  });
});
```

- [ ] **Step 2: Vérifier qu'ils échouent**

Run: `npx vitest run tests/render/bgevents.test.js`
Expected: FAIL — module absent

- [ ] **Step 3: Implémenter le module**

Créer `src/render/bgevents.js` :

```js
import { CONFIG } from '../config.js';
import { foudreAlpha } from '../game/bgevents.js';

function drawFoudre(ctx, e) {
  // Dessiné AVANT le premier plan : le ciel s'illumine, les silhouettes
  // near restent sombres (contre-jour plausible).
  ctx.fillStyle = `rgba(210,225,255,${foudreAlpha(e).toFixed(3)})`;
  ctx.fillRect(0, 0, CONFIG.WIDTH, CONFIG.HEIGHT);
}

function drawEtoile(ctx, e) {
  const hx = e.x0 + e.vx * e.t;
  const hy = e.y0 + e.vy * e.t;
  const norm = Math.hypot(e.vx, e.vy);
  const ux = e.vx / norm;
  const uy = e.vy / norm;
  for (let i = 0; i < 7; i += 1) {
    ctx.globalAlpha = (1 - e.t / e.dur) * (1 - i / 7);
    ctx.fillStyle = i === 0 ? '#eaf6ff' : '#9fd8ff';
    ctx.fillRect(Math.round(hx - ux * i * 5), Math.round(hy - uy * i * 5), 2, 2);
  }
  ctx.globalAlpha = 1;
}

function drawOiseaux(ctx, e) {
  const speed = (CONFIG.WIDTH + 60) / e.dur; // la durée EST la traversée
  ctx.fillStyle = 'rgba(40,26,52,0.9)';
  for (let i = 0; i < 5; i += 1) {
    const x = e.dir > 0
      ? -20 + speed * e.t - i * 14
      : CONFIG.WIDTH + 20 - speed * e.t + i * 14;
    const y = e.baseY + (i % 2) * 7 - 3 + Math.sin(e.t * 2 + i) * 4;
    const flap = Math.floor(e.t * 6 + i) % 2; // battement 2 frames
    ctx.fillRect(Math.round(x), Math.round(y), 2, 1);
    ctx.fillRect(Math.round(x) + 2, Math.round(y) - (flap ? 1 : 0), 2, 1);
    ctx.fillRect(Math.round(x) + 4, Math.round(y), 2, 1);
  }
}

function drawTorchere(ctx, e) {
  const env = Math.min(1, e.t / 0.4, (e.dur - e.t) / 0.4); // fondu in/out
  const pulse = 0.5 + 0.5 * Math.sin(e.t * 6);
  const base = 0.26 * env * (0.6 + 0.4 * pulse);
  ctx.fillStyle = '#7dffb0';
  for (const [r, a] of [[14, base * 0.35], [9, base * 0.6], [5, base]]) {
    ctx.globalAlpha = a;
    ctx.fillRect(e.spot.x - r, e.spot.y - r, r * 2, r * 2);
  }
  ctx.globalAlpha = 1;
}

export function renderBgEvents(ctx, world) {
  const e = world.bgEvents.event;
  if (!e) return;
  if (e.kind === 'foudre') drawFoudre(ctx, e);
  else if (e.kind === 'etoile') drawEtoile(ctx, e);
  else if (e.kind === 'oiseaux') drawOiseaux(ctx, e);
  else if (e.kind === 'torchere') drawTorchere(ctx, e);
  // 'rafale' : boost des twinkles, rendu à l'étape 2a du renderer.
}
```

- [ ] **Step 4: Vérifier que les tests passent**

Run: `npx vitest run tests/render/bgevents.test.js`
Expected: PASS (6 tests)

- [ ] **Step 5: Commit**

```bash
git add src/render/bgevents.js tests/render/bgevents.test.js
git commit -m "feat(render): dessin des événements de fond (foudre, étoile, oiseaux, torchère)"
```

---

### Task 3: Intégration world + renderer

**Files:**
- Modify: `src/game/world.js` (imports, `createWorld`, `applyBgSet`, `updateWorld`)
- Modify: `src/render/renderer.js` (appel couche + boost twinkles)
- Test: `tests/game/world.test.js` (nouveau describe)

**Interfaces:**
- Consumes: `createBgEvents/updateBgEvents/resetBgEvents/rafaleAlpha` (Task 1), `renderBgEvents` (Task 2).
- Produces: `world.bgEvents` disponible pour le renderer ; reset garanti à chaque `applyBgSet` (donc `startLevel` et vitrine du menu).

- [ ] **Step 1: Écrire les tests qui échouent**

Ajouter à la fin de `tests/game/world.test.js` (dans le describe racine `world`) :

```js
  describe('événements de fond (bgEvents)', () => {
    it('createWorld arme les événements : timer dans [6, 12), aucun actif', () => {
      const w = createWorld(fakeStorage());
      expect(w.bgEvents.event).toBe(null);
      expect(w.bgEvents.timer).toBeGreaterThanOrEqual(6);
      expect(w.bgEvents.timer).toBeLessThan(12);
    });

    it('updateWorld fait vivre les événements même au MENU (vitrine)', () => {
      const w = createWorld(fakeStorage());
      const before = w.bgEvents.timer;
      updateWorld(w, 1 / 60);
      expect(w.bgEvents.timer).toBeLessThan(before);
    });

    it('startLevel coupe l événement en cours (pas d étoile filante sur la tempête)', () => {
      const w = createWorld(fakeStorage());
      w.bgEvents.event = { kind: 'etoile', t: 0.1, dur: 0.7, x0: 0, y0: 0, vx: 260, vy: 110 };
      startLevel(w, 7);
      expect(w.bgEvents.event).toBe(null);
      expect(w.bgEvents.timer).toBeGreaterThanOrEqual(6);
    });

    it('un événement finit par se déclencher en jeu puis s éteint', () => {
      const w = createWorld(fakeStorage());
      startLevel(w, 10);
      w.sm.to(States.PLAY);
      let seen = false;
      for (let i = 0; i < 13 * 60 && !seen; i += 1) {
        updateWorld(w, 1 / 60);
        if (w.bgEvents.event) seen = true;
        if (w.sm.get() !== States.PLAY) w.sm.to(States.PLAY); // on ignore la mort du robot
      }
      expect(seen).toBe(true);
    });
  });
```

- [ ] **Step 2: Vérifier qu'ils échouent**

Run: `npx vitest run tests/game/world.test.js`
Expected: FAIL — `w.bgEvents` undefined

- [ ] **Step 3: Intégrer dans world.js**

Dans `src/game/world.js` :

1. Import : `import { createBgEvents, updateBgEvents, resetBgEvents } from './bgevents.js';`
2. Champ dans le littéral de `createWorld` (à côté de `twinkles`) : `bgEvents: createBgEvents(Math.random),`
3. Compléter `applyBgSet` (sans risque : `applyBgSet` est appelé depuis `createWorld` APRÈS la création du littéral, donc `world.bgEvents` et `world.rand` existent) :

```js
function applyBgSet(world, set) {
  world.bgSet = set;
  world.layers[0].speedFactor = CONFIG.BG_FAR_SPEED[set];
  world.layers[0].offset = 0;
  resetBgEvents(world.bgEvents, world.rand); // nouvel écran, nouveau tirage
}
```

4. Dans `updateWorld`, juste après `updateAmbiance(...)` :

```js
  updateBgEvents(world.bgEvents, dt, world.bgSet, world.rand);
```

- [ ] **Step 4: Intégrer dans renderer.js**

Dans `src/render/renderer.js` :

1. Imports : `import { renderBgEvents } from './bgevents.js';` et compléter l'import twinkle : `import { rafaleAlpha } from '../game/bgevents.js';`
2. Entre l'étape 1 (far, les deux `drawImage`) et l'étape 2 (near) :

```js
  // 1b. Événement de fond (foudre, étoile filante, oiseaux, torchère) —
  // derrière le premier plan : les silhouettes restent en contre-jour.
  renderBgEvents(ctx, world);
```

3. Étape 2a, remplacer la ligne d'alpha des twinkles :

```js
    ctx.globalAlpha = Math.max(twinkleAlpha(point, world.tick), rafaleAlpha(world.bgEvents, point.x));
```

- [ ] **Step 5: Vérifier que tout passe (suite complète)**

Run: `npx vitest run`
Expected: PASS — 291 tests existants + les nouveaux, 0 échec

- [ ] **Step 6: Commit**

```bash
git add src/game/world.js src/render/renderer.js tests/game/world.test.js
git commit -m "feat(world): les événements de fond vivent par décor (update, reset, couche de rendu)"
```

---

### Task 4: Smoke Playwright + vérification visuelle

**Files:**
- Create: `<worktree>/.superpowers/smokes/bgevents-smoke.mjs` (hors repo, comme les autres smokes)

**Interfaces:**
- Consumes: serveur Vite `npx vite --port 5199 --strictPort` lancé depuis `<worktree>/1st_Slop` ; codes de niveau via `localStorage jetpackbot.bestLevel` (1/3/5/7/10).
- Produces: captures par décor pendant l'événement, à juger visuellement (puis gate Jael).

- [ ] **Step 1: Écrire le smoke**

```js
// Smoke événements de fond — Math.random forcé à 0 : timer = 6 s pile,
// params déterministes. Rafale de captures autour de t=6 s pour attraper
// même la foudre (0,5 s). À lancer : node bgevents-smoke.mjs <outdir>
import { chromium } from 'file:///C:/Users/pattyn/AppData/Local/npm-cache/_npx/e41f203b7505f1fb/node_modules/playwright/index.mjs';

const BASE = 'http://localhost:5199';
const OUT = process.argv[2] || '.';
const errors = [];
const browser = await chromium.launch();

async function scenario(name, bestLevel) {
  const ctx = await browser.newContext({ viewport: { width: 480, height: 864 } });
  const page = await ctx.newPage();
  page.on('pageerror', (e) => errors.push(`[${name}] pageerror: ${e.message}`));
  page.on('console', (m) => { if (m.type() === 'error') errors.push(`[${name}] console: ${m.text()}`); });
  await page.addInitScript((lv) => {
    Math.random = () => 0; // déterministe : délai 6 s, premier spot/trajectoire
    if (lv) localStorage.setItem('jetpackbot.bestLevel', String(lv));
    else localStorage.clear();
  }, bestLevel);
  await page.goto(BASE);
  await page.waitForTimeout(1800);
  if (bestLevel) await page.keyboard.press('ArrowDown'); // CONTINUE
  await page.keyboard.press('Enter'); // démarre (applyBgSet -> timer 6 s)
  // Survie pendant l'attente : petites poussées régulières.
  const t0 = Date.now();
  while (Date.now() - t0 < 5600) {
    await page.keyboard.down('Space');
    await page.waitForTimeout(150);
    await page.keyboard.up('Space');
    await page.waitForTimeout(190);
  }
  for (let shot = 0; shot < 6; shot += 1) {
    await page.screenshot({ path: `${OUT}/${name}-${shot}.png` });
    await page.keyboard.down('Space');
    await page.waitForTimeout(80);
    await page.keyboard.up('Space');
    await page.waitForTimeout(60);
  }
  await ctx.close();
}

await scenario('bg0-rafale', null);
await scenario('bg1-torchere', 3);
await scenario('bg2-oiseaux', 5);
await scenario('bg3-foudre', 7);
await scenario('bg4-etoile', 10);
await browser.close();
console.log(errors.length ? 'ERRORS:\n' + errors.join('\n') : 'ZERO console/page errors');
```

- [ ] **Step 2: Lancer et juger**

Run (serveur :5199 déjà lancé) : `node ../.superpowers/smokes/bgevents-smoke.mjs <outdir>`
Expected: `ZERO console/page errors` + sur les captures : le flash foudre illumine le ciel SANS masquer les portes ; l'étoile file derrière la station ; oiseaux/torchère/rafale visibles. Si l'événement est raté par le timing (mort du robot avant 6 s), relancer — Math.random=0 rend les portes très permissives (gap plein centre).

- [ ] **Step 3: Ajuster ce qui choque (TORCH_SPOTS, vitesses, alphas)**

Modifier UNIQUEMENT les constantes de `src/game/bgevents.js` / `src/render/bgevents.js`, relancer les tests (`npx vitest run`) puis le smoke.

- [ ] **Step 4: Commit final + gate Jael**

```bash
git add -A src/ tests/
git commit -m "polish(bgevents): calibrage visuel des événements de fond"
```

Puis : gate visuel Jael en jeu (spec : effet « vivant », foudre spectaculaire, lisibilité des portes intacte).
