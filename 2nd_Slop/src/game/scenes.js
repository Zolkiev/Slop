// Scènes de vitrail : certains grands moments (cartes `art: '<clé>'`)
// montrent l'événement dans l'arche plutôt que le buste de l'orateur —
// comme les verrières d'église racontent des scènes, pas que des saints.
// Fichiers : assets/scenes/<clé>.png (128x128, même pipeline que les portraits).

export const SCENES = [
  'cerf-blanc', // roche.chasse.blanche — le cerf féerique traverse la lande
  'graal-vision', // camelot.graal.vision — la coupe de lumière
  'fourreau-vole', // chute.fourreau.vole — la silhouette qui fuit à l'aube
  'camlann', // chute.camlann.duel — le champ des morts
  'barque-avalon', // avalon.barque / avalon.morgane.retour — la traversée
];
